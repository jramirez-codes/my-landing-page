// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['"Roboto"', 'sans-serif'],
        // You can also customize serif and mono if needed
        // serif: ['Georgia', 'serif'],
        // mono: ['ui-monospace', 'SFMono-Regular', ...],
      },
    },
  },
  plugins: [],
};
